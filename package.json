{
  "name": "@escodb/buffer-codec",
  "version": "0.1.0",
  "description": "Convert typed arrays to/from hex, base32, base64, and other base representations",
  "author": "James Coglan <jcoglan@gmail.com> (http://jcoglan.com/)",
  "keywords": [
    "typed array",
    "buffer",
    "binary",
    "hex",
    "base32",
    "base64"
  ],
  "license": "Apache-2.0",
  "homepage": "https://github.com/escodb/buffer-codec",
  "repository": "github:escodb/buffer-codec",
  "bugs": "https://github.com/escodb/buffer-codec/issues",
  "files": [
    "lib"
  ],
  "main": "./lib/index",
  "devDependencies": {
    "chai": "^4.5.0",
    "esbuild": "^0.27.2",
    "mocha": "^11.7.5"
  },
  "scripts": {
    "test": "mocha",
    "test:web": "esbuild --bundle test/web/main.js --outfile=test/web/bundle.js --watch --servedir=."
  }
}
